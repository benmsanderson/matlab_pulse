function [t tp]=r_rsp(pt,fcg,ts)

if nargin<3
    ts=1:numel(fcg);
end

sns=pt(7:9);
tt=pt(4:6);

dfcg=diff([0 fcg']);


tf=(-exp(-repmat(ts,3,1)./repmat(tt(:),1,numel(ts))));
tp=filter(dfcg,1,tf(:,:)'*diag(sns));

t=sum(tp,2);